namespace Shell_Sort
{
    internal class DataPoint
    {
        private int length;
        private float v;

        public DataPoint(int length, float v)
        {
            this.length = length;
            this.v = v;
        }
    }
}